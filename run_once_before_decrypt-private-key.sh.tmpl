#!/bin/sh

if [ ! -f "${HOME}/.config/chezmoi/key.age" ]; then
    mkdir -p "${HOME}/.config/chezmoi"
    chezmoi age decrypt --output "${HOME}/.config/chezmoi/key.age" --passphrase "{{ .chezmoi.sourceDir }}/key.age"
    chmod 600 "${HOME}/.config/chezmoi/key.age"
fi
